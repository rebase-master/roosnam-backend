#!/usr/bin/env ruby

# This script is no longer used as solid_queue has been removed.
# If you need background job processing, configure a different ActiveJob adapter
# (e.g., sidekiq, delayed_job, etc.) and update this script accordingly.
